version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site — no build step required"
  artifacts:
    baseDirectory: frontend
    files:
      - "**/*"
  cache:
    paths: []
